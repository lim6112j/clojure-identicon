(ns identicon.core-test
  (:require [clojure.test :refer :all]
            [identicon.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))

(deftest from-hex-dec-test
  (is (= 1 1)))
